import { dinamicSearchPokemon } from './data.js';

describe('dinamicSearchPokemon', () => {
  it('dinamicSearchPokemon debería ser una función', () => {
    expect(typeof dinamicSearchPokemon).toBe('function');
  });
});
